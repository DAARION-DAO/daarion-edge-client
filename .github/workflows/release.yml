name: "DAARION Edge Client — Sovereign Release"

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # ── Desktop ──────────────────────────────────────────────
          - platform: "macos-latest"
            args: "--target aarch64-apple-darwin"
            rust_target: "aarch64-apple-darwin"
            label: "macos-arm64"

          - platform: "macos-latest"
            args: "--target x86_64-apple-darwin"
            rust_target: "x86_64-apple-darwin"
            label: "macos-x86_64"

          - platform: "ubuntu-22.04"
            args: ""
            rust_target: ""
            label: "linux-x86_64"

          - platform: "windows-latest"
            args: ""
            rust_target: ""
            label: "windows-x86_64"

          # ── Mobile ───────────────────────────────────────────────
          - platform: "ubuntu-22.04"
            args: "--target aarch64-linux-android"
            rust_target: "aarch64-linux-android"
            label: "android-arm64"

          # iOS requires macOS + Xcode (self-hosted) — skipping free-tier
          # - platform: "macos-latest"
          #   args: "--target aarch64-apple-ios"
          #   rust_target: "aarch64-apple-ios"
          #   label: "ios-arm64"

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: >-
            ${{
              matrix.label == 'macos-arm64' && 'aarch64-apple-darwin,x86_64-apple-darwin' ||
              matrix.label == 'macos-x86_64' && 'aarch64-apple-darwin,x86_64-apple-darwin' ||
              matrix.label == 'android-arm64' && 'aarch64-linux-android' ||
              ''
            }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"

      # ── Linux system deps ──────────────────────────────────────
      - name: Install Linux system dependencies
        if: matrix.platform == 'ubuntu-22.04' && matrix.label == 'linux-x86_64'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libasound2-dev \
            libssl-dev \
            pkg-config

      # ── Android setup ──────────────────────────────────────────
      - name: Setup Java (Android)
        if: matrix.label == 'android-arm64'
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Android NDK
        if: matrix.label == 'android-arm64'
        uses: android-actions/setup-android@v3

      - name: Install Android target
        if: matrix.label == 'android-arm64'
        run: |
          rustup target add aarch64-linux-android
          cargo install tauri-cli --version "^2" 2>/dev/null || true
          # Install Android NDK tools
          sdkmanager "ndk;27.0.12077973" "platforms;android-34" "build-tools;34.0.0" --sdk_root=$ANDROID_HOME

      - name: Guard — Android signing secrets required
        if: matrix.label == 'android-arm64'
        env:
          HAS_KEYSTORE: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          HAS_KS_PASS: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          HAS_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          HAS_KEY_PASS: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          if [ -z "$HAS_KEYSTORE" ] || \
             [ -z "$HAS_KS_PASS" ] || \
             [ -z "$HAS_KEY_ALIAS" ] || \
             [ -z "$HAS_KEY_PASS" ]; then
            echo "::error::Android signing secrets are required for release APK."
            echo "Required secrets: ANDROID_KEYSTORE_BASE64, ANDROID_KEYSTORE_PASSWORD, ANDROID_KEY_ALIAS, ANDROID_KEY_PASSWORD"
            exit 1
          fi
          echo "✅ All Android signing secrets are present."

      - name: Init Tauri Android project
        if: matrix.label == 'android-arm64'
        run: |
          npm ci
          cargo tauri android init 2>/dev/null || true

      - name: Configure Android release signing
        if: matrix.label == 'android-arm64'
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          # Reconstruct keystore from base64 secret into a temp file
          echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > "$RUNNER_TEMP/daarion-edge-release.keystore"
          echo "✅ Keystore reconstructed at $RUNNER_TEMP/daarion-edge-release.keystore"

          # Write keystore.properties for Gradle signing config
          ANDROID_PROJECT_DIR="src-tauri/gen/android"
          {
            echo "keyAlias=$ANDROID_KEY_ALIAS"
            echo "password=$ANDROID_KEY_PASSWORD"
            echo "storePassword=$ANDROID_KEYSTORE_PASSWORD"
            echo "storeFile=$RUNNER_TEMP/daarion-edge-release.keystore"
          } > "$ANDROID_PROJECT_DIR/keystore.properties"
          echo "✅ keystore.properties written to $ANDROID_PROJECT_DIR/"

          # Patch build.gradle.kts to use release signing config
          GRADLE_FILE="$ANDROID_PROJECT_DIR/app/build.gradle.kts"
          if [ -f "$GRADLE_FILE" ]; then
            # Write signing config snippet to temp file
            cat > "$RUNNER_TEMP/signing-config.txt" << 'SIGNING_EOF'
              signingConfigs {
                  create("release") {
                      val keystorePropertiesFile = rootProject.file("keystore.properties")
                      val keystoreProperties = Properties()
                      if (keystorePropertiesFile.exists()) {
                          keystoreProperties.load(keystorePropertiesFile.inputStream())
                          keyAlias = keystoreProperties["keyAlias"].toString()
                          keyPassword = keystoreProperties["password"].toString()
                          storeFile = file(keystoreProperties["storeFile"].toString())
                          storePassword = keystoreProperties["storePassword"].toString()
                      }
                  }
              }
          SIGNING_EOF

            # Use python to perform precise file manipulation
            python3 -c "
          import re
          with open('$GRADLE_FILE', 'r') as f:
              content = f.read()
          with open('$RUNNER_TEMP/signing-config.txt', 'r') as f:
              signing = f.read()
              
          # Insert signing configs before buildTypes if not already present
          if 'signingConfigs {' not in content:
              content = content.replace('buildTypes {', signing + '\n    buildTypes {', 1)
          
          # Update release buildType to use the signing config
          if 'signingConfig = signingConfigs.getByName(\"release\")' not in content:
              content = content.replace(
                  'getByName(\"release\") {',
                  'getByName(\"release\") {\n                signingConfig = signingConfigs.getByName(\"release\")',
                  1
              )
          with open('$GRADLE_FILE', 'w') as f:
              f.write(content)
          "

            echo "✅ build.gradle.kts patched with release signing config"
            echo "--- Patched signing section ---"
            grep -A5 'signingConfigs' "$GRADLE_FILE" || true
          else
            echo "::warning::build.gradle.kts not found at $GRADLE_FILE — signing config not applied"
          fi

      # ── Node.js setup ──────────────────────────────────────────
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "package-lock.json"

      - name: Install frontend dependencies
        run: npm ci

      # ── Desktop Build (tauri-action) ────────────────────────────
      - name: Build Tauri Desktop App
        if: matrix.label != 'android-arm64'
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          includeRelease: true
          args: ${{ matrix.args }}

      # ── Rename and Stage Desktop Artifacts ────────────────────────
      - name: Rename and Stage Desktop Artifacts
        if: matrix.label != 'android-arm64'
        env:
          ARTIFACT_LABEL: ${{ matrix.label }}
          RUST_TARGET: ${{ matrix.rust_target }}
        run: |
          node scripts/prepare-release-artifacts.cjs

      # ── Upload Desktop Artifacts to Release ──────────────────────
      - name: Upload Desktop Artifacts to Release
        if: matrix.label != 'android-arm64'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            dist/artifacts/*
          prerelease: true
          body: |
            ## 🌌 DAARION Edge Client

            ### 📱 Downloads
            | Platform | File | Status |
            |----------|------|--------|
            | Windows x64 (MSI) | `Daarion.Edge_<version>_x64_en-US.msi` | ⚠️ Tester/Canary — boot proof pending |
            | Windows x64 (Setup) | `Daarion.Edge_<version>_x64-setup.exe` | ⚠️ Tester/Canary — boot proof pending |
            | macOS (Apple Silicon) | `Daarion.Edge_<version>_aarch64.dmg` | ⚠️ Tester/Canary — signing and notarization proof required |
            | macOS (Intel) | `Daarion.Edge_<version>_x64.dmg` | ⚠️ Tester/Canary — signing and notarization proof required |
            | Linux | `Daarion.Edge_<version>_amd64.AppImage` | ⚠️ Tester/Canary — launch proof pending |
            | Android | `Daarion.Edge_<version>_android_universal_release.apk` | ⚠️ Tester/Canary — APK signature and install proof pending |

            ### ⚠️ Release Validation Status
            - `build exists` ≠ `install proof`
            - `install proof` ≠ `boot proof`
            - `boot proof` ≠ `worker proof`
            - Per-platform `SHA256SUMS-<platform>.txt` files are generated by the release artifact staging script.
            - Windows testers: check `%APPDATA%/DAARION Edge/logs/boot.log` if the app closes unexpectedly.
            - Android APK signing is only confirmed when APK signature proof is attached. Sideload installation requires enabling unknown sources.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ── Android Build (signed) ─────────────────────────────────────
      - name: Build Tauri Android APK (signed)
        if: matrix.label == 'android-arm64'
        run: |
          cargo tauri android build --apk 2>&1
        env:
          NDK_HOME: ${{ env.ANDROID_NDK_ROOT }}

      # ── Rename and Stage Android Artifacts ────────────────────────
      - name: Rename and Stage Android Artifacts
        if: matrix.label == 'android-arm64'
        env:
          ARTIFACT_LABEL: ${{ matrix.label }}
          RUST_TARGET: ${{ matrix.rust_target }}
        run: |
          node scripts/prepare-release-artifacts.cjs

      # ── Upload signed Android APK to Release ──────────────────────
      - name: Upload signed Android APK to Release
        if: matrix.label == 'android-arm64'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            dist/artifacts/*
          prerelease: true
          body: |
            ## 🌌 DAARION Edge Client

            ### 📱 Downloads
            | Platform | File | Status |
            |----------|------|--------|
            | Windows x64 (MSI) | `Daarion.Edge_<version>_x64_en-US.msi` | ⚠️ Tester/Canary — boot proof pending |
            | Windows x64 (Setup) | `Daarion.Edge_<version>_x64-setup.exe` | ⚠️ Tester/Canary — boot proof pending |
            | macOS (Apple Silicon) | `Daarion.Edge_<version>_aarch64.dmg` | ⚠️ Tester/Canary — signing and notarization proof required |
            | macOS (Intel) | `Daarion.Edge_<version>_x64.dmg` | ⚠️ Tester/Canary — signing and notarization proof required |
            | Linux | `Daarion.Edge_<version>_amd64.AppImage` | ⚠️ Tester/Canary — launch proof pending |
            | Android | `Daarion.Edge_<version>_android_universal_release.apk` | ⚠️ Tester/Canary — APK signature and install proof pending |

            ### ⚠️ Release Validation Status
            - `build exists` ≠ `install proof`
            - `install proof` ≠ `boot proof`
            - `boot proof` ≠ `worker proof`
            - Per-platform `SHA256SUMS-<platform>.txt` files are generated by the release artifact staging script.
            - Windows testers: check `%APPDATA%/DAARION Edge/logs/boot.log` if the app closes unexpectedly.
            - Android APK signing is only confirmed when APK signature proof is attached. Sideload installation requires enabling unknown sources.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Clean up keystore
        if: matrix.label == 'android-arm64' && always()
        run: |
          rm -f "$RUNNER_TEMP/daarion-edge-release.keystore"
          rm -f src-tauri/gen/android/keystore.properties
          echo "✅ Signing artifacts cleaned up"
